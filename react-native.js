'use strict';

// @TODO: this one needs more work
module.exports = {
  extends: ['@react-native-community', './react.js'],
  plugins: ['react-native'],
  parserOptions: {
    ecmaFeatures: {
      jsx: true,
    },
  },
  rules: {
  },
};
